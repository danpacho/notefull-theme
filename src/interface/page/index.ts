/**
 * @note this is useful, when you have to distinguish page or set different style or layout for each pages in `_app.tsx`.
 */
export type PageType = "Home" | "Category" | "Post" | "Profile" | "Error"
