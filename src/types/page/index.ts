/**
 * @note this is usefull, when you have to distinguish page or set diffrent style or layout for each pages in `_app.tsx`.
 */
export type PageType = "Home" | "Category" | "Post" | "Profile" | "Error"
